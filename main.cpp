#include <iostream>
#include "BitBoard.h"
#include <bitset>
using namespace std;

int main() {
    //Tests only


    /**
    BitBoard s;
    int len;
    BitBoard* y = s.getLegalBoards(0, &len);
    
    delete[] y;
    */
    
    return 0;
}